<div class="bg-light p-5 rounded-lg m-3">
    <h1 class="display-2">Bem vindo aqui falaremos de Node js</h1>
    <p class="lead">Create an account and add a post</p>
    <hr class="my-5">
    <p>leave your doubt, comment..</p>
    <a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Crie uma conta</a>
</div>
<hr>

<h2>Postagens Recentes:</h2>

{{#each postagens}}

<div class="card mt-3">
    <div class="card-body">
        <h3> {{titulo}} </h3>
        <p > Desc: {{descricao}} </p>
        <a href="/postagem/{{slug}}" ><button class="btn btn-success">Leia mais</button></a>
        <hr>
        <small> {{categoria.nome}} </small><br>
        <small>Data de publicação: {{data}} </small>
        <hr>
    </div>
</div>

{{else}}
<h5>Nenhuma postagem</h5>
{{/each}}