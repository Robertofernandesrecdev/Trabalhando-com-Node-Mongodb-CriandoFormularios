{
    "version": 2,
    "public": true,
    "builds":[
        {
            "": "./app.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "routes": "/(.*)",
            "dest": "/"
        }
    ]
}